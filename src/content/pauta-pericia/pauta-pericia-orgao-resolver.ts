/**
 * OBSOLETO — o Painel de Perícias passou a extrair os dados direto de cada
 * processo da unidade (universo = tarefas + etiquetas, igual ao PREVJUD), o
 * que dispensa resolver órgão julgador e filtrar por unidade. Este arquivo
 * pode ser removido do repositório; ficou como módulo vazio só porque o
 * harness não deleta arquivos.
 */
export {};
